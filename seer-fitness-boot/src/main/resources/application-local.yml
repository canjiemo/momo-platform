# Development Environment Configuration

spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/fitness-demo?stringtype=unspecified
    username: admin
    password: 123456
    druid:
      stat-view-servlet:
        enabled: true
        login-username: admin
        login-password: admin123

  flyway:
    enabled: false

  data:
    redis:
      host: localhost
      port: 30069
      password: Seer@bigdata

