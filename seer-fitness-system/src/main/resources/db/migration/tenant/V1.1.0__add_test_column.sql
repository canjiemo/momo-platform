-- ====================================================================================================
-- Flyway Migration - Tenant Schema V1.1.0
-- 说明：测试升级功能 - 为sys_user表添加测试字段
-- 版本：1.1.0
-- 创建时间：2025-10-18
-- 适用对象：所有租户Schema
-- ====================================================================================================
-- 变更内容：
--   1. 为sys_user表添加test_field字段（用于测试升级功能）
-- ====================================================================================================

-- 添加测试字段到sys_user表
ALTER TABLE sys_user ADD COLUMN IF NOT EXISTS test_field VARCHAR(100);

COMMENT ON COLUMN sys_user.test_field IS '测试字段（用于验证Schema升级功能）';

-- 记录升级信息
DO $$
BEGIN
    RAISE NOTICE 'Schema升级到V1.1.0完成：已添加test_field字段';
END $$;
