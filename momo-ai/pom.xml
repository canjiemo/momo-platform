<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>io.github.canjiemo</groupId>
        <artifactId>momo-platform</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>momo-ai</artifactId>
    <description>AI 智能查询模块，支持自然语言查询数据库，可插拔 AI Provider</description>

    <dependencies>
        <!-- myjdbc: SQL执行 + 租户隔离 + 逻辑删除，Map.class 返回 -->
        <dependency>
            <groupId>io.github.canjiemo</groupId>
            <artifactId>myjdbc-spring-boot-starter</artifactId>
            <version>${myjdbc.version}</version>
        </dependency>

        <!-- mymvc: Controller 基类 + 统一响应封装 -->
        <dependency>
            <groupId>io.github.canjiemo</groupId>
            <artifactId>mymvc-spring-boot-starter</artifactId>
            <version>${mymvc.version}</version>
        </dependency>

        <!-- framework: @RequireAuth 注解、BusinessException 等基础设施 -->
        <dependency>
            <groupId>io.github.canjiemo</groupId>
            <artifactId>momo-framework</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring AI Ollama（2.0.0 GA，版本由根 pom 的 spring-ai-bom 统一管理） -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-model-ollama</artifactId>
        </dependency>

        <!-- Spring AI OpenAI（兼容阿里百炼 DashScope OpenAI 协议，底层为官方 OpenAI SDK） -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-model-openai</artifactId>
        </dependency>

        <!-- 官方 OpenAI Java SDK 的 OkHttp 实现（提供 OpenAIOkHttpClient，
             spring-ai 仅传递 openai-java-core，需显式引入实现模块；版本对齐 core 4.39.1） -->
        <dependency>
            <groupId>com.openai</groupId>
            <artifactId>openai-java-client-okhttp</artifactId>
            <version>4.39.1</version>
        </dependency>

        <!-- pgvector Java 客户端（版本由根 pom 统一管理） -->
        <dependency>
            <groupId>com.pgvector</groupId>
            <artifactId>pgvector</artifactId>
        </dependency>

        <!-- JSqlParser: SQL 安全校验 (myjdbc 已引入，不重复声明版本) -->
        <dependency>
            <groupId>com.github.jsqlparser</groupId>
            <artifactId>jsqlparser</artifactId>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>com.alibaba.fastjson2</groupId>
            <artifactId>fastjson2</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
